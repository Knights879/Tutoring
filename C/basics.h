// For standard library files, `<...>` surround the file name.
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>

// Use a header file for managing and including (called imports in other languages),
// things like `typedefs`, constants, and global variables, and functional prototypes.


/* FUNCTIONAL PROTOTYPES */

// Prints "Hello, World!"
void hello_world(void);
